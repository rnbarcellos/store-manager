const showAllProducts = async (req, res) => {
  console.log(req, res);
};

const showProductById = async (req, res) => {
  console.log(req, res);
};

module.exports = {
  showAllProducts,
  showProductById,
};
